# 現在の便秘問診システムの仕組み（ChatGPT相談用）

このMarkdownは、現在このリポジトリで作っている「小児慢性機能性便秘症の診察前問診システム」を、別テーマのQRコード問診システムへ応用するために、ChatGPTへ共有する目的でまとめたものです。

相談先に貼る場合、実際のGoogle Apps Script URL、スプレッドシートID、script IDなどは伏せても設計相談は可能です。

---

## 1. システムの目的

現在のシステムは、小児慢性機能性便秘症の患者・保護者が診察前にスマートフォン等で問診を入力し、医師が診察前に要点を確認できるようにするためのものです。

このシステムは、診断や処方判断を自動化するものではありません。役割は以下です。

- 患者・保護者から診察前に必要情報を集める
- 回答内容を構造化する
- 医師向けに要点、追加確認項目、注意点を表示する
- 患者向けには相談準備用メモを出す
- Googleスプレッドシートに受診ごとの履歴として保存する
- 必要に応じてChatGPTへ貼るための医師向けコンテキストを生成する

他テーマへ流用する場合も、「患者が先に入力し、医師が診察・説明前に確認する」用途を想定しています。

---

## 2. 全体構成

主な構成は、静的HTML/JavaScriptの患者画面と、Google Apps Script + Googleスプレッドシートの保存・医師画面です。

```text
患者・保護者
  ↓ QRコードまたはURL
HTML / JavaScript 問診画面
  ↓ POST / GET
Google Apps Script Web App
  ↓ 読み書き
Googleスプレッドシート
  ↓
医師用画面 / 履歴表示 / ChatGPT相談用テキスト
```

主要ファイルは以下です。

- `constipation-ai-mvp/index.html`: 患者向け問診画面の入口
- `constipation-ai-mvp/styles.css`: 画面デザイン
- `constipation-ai-mvp/src/app.js`: 画面遷移、URLパラメータ処理、保存処理
- `constipation-ai-mvp/src/questionnaire.js`: 質問定義、条件分岐、サマリー生成、保存用JSON生成
- `constipation-ai-mvp/visit-link.html`: 患者ID入りURL・QRコード作成画面
- `constipation-ai-mvp/src/visit-link.js`: QRコード生成ロジック
- `constipation-ai-mvp/history-link.html`: 医師用リンク作成画面
- `constipation-ai-mvp/src/history-link.js`: 医師用画面・履歴・ChatGPT用URL作成
- `apps-script/Code.gs`: Google Apps Script本体
- `apps-script/appsscript.json`: Apps Script設定

---

## 3. 患者側の基本フロー

患者側は、QRコードまたはURLから `index.html` を開きます。

基本フローは以下です。

1. QRコードまたはURLで問診画面を開く
2. 患者ID、来院トークン、年齢プロファイルなどをURLから取得する
3. 必要に応じてGoogle Apps Scriptから患者台帳情報を取得する
4. イントロ画面で「薬の量や治療方針はこの画面では決めない」と説明する
5. 年齢プロファイルに応じた質問セットを表示する
6. 回答内容に応じて追加質問を条件分岐で表示する
7. 必要に応じて直近日誌を入力する
8. 患者用メモ、回答コード、医師向けサマリー、保存用JSONを生成する
9. 患者IDと来院トークンがある場合、Google Apps Scriptへ自動保存する
10. 保存完了後に「画面を閉じても大丈夫」と表示する

保存できない場合でも、患者用メモ・回答コード・JSONを画面上に残し、受付または医師に見せられるようにしています。

---

## 4. QRコード運用

QRコードには大きく2種類の運用があります。

### 4.1 来院ごとの個別QR

`visit-link.html` で、患者IDと来院トークン入りのURLを作成します。

URLパラメータ例:

```text
index.html?patient_id=00123&visit_token=ABCD
```

扱う値:

- `patient_id`: 5桁の数字
- `visit_token`: 英数字、最大12文字

患者IDと来院トークンがある場合だけ、自動保存を行います。

### 4.2 固定QR + 当日確認コード

`app.js` には固定QRモードも実装されています。

URLパラメータ例:

```text
index.html?mode=fixed&form=constipation
```

この場合、患者は画面で以下を入力します。

- 診察券番号
- 本日の確認コード
- 生年月日（初回登録時のみ）

Google Apps Script側で `fixedQrAuth` を実行し、当日の確認コードと患者情報を確認します。認証後、来院トークンを自動生成して問診へ進みます。

現在の固定QR対象フォーム名として、コード上は以下が定義されています。

- `constipation`
- `asthma`
- `atopic_dermatitis`

ただし実際に認証を通す処理は、現状では便秘用の `constipation` が中心です。他テーマへ流用する場合は、フォーム名、認証後の質問セット、保存先の扱いを追加する必要があります。

---

## 5. 年齢プロファイルと質問セット

現在は便秘症向けに、年齢プロファイルで質問セットを切り替えています。

- `infant`: 0-1歳向け
- `toddler`: 2-3歳向け
- `child`: 4歳以降向け
- `unknown`: 年齢不明時。既定では2-3歳向け

質問セットのバージョンは以下です。

```text
infant  -> infant-prototype-v1
toddler -> toddler-mvp-v1
child   -> child-prototype-v1
unknown -> toddler-mvp-v1
```

年齢プロファイルは以下の順で決まります。

1. URLパラメータ `age_profile` または `profile`
2. 患者IDをもとにGoogle Apps Scriptの `patientProfileData` から取得
3. 患者台帳の生年月日からApps Script側で計算
4. 不明な場合は `toddler` 相当へフォールバック

他テーマへ流用する場合は、年齢ではなく「対象区分」「状態区分」「初回/再診」「処置前/処置後」などで質問セットを切り替えてもよい設計です。

---

## 6. 質問定義と条件分岐

質問定義は `questionnaire.js` にまとまっています。

各質問は概ね以下の情報を持ちます。

```js
{
  label: "画面に表示する質問文",
  summaryLabel: "サマリー用の短い項目名",
  help: "補足説明",
  type: "single" | "multi" | "text",
  required: true | false,
  options: ["選択肢1", "選択肢2"],
  group: "basic" | "condition" | "medicine" など
}
```

患者画面では以下の入力形式を使っています。

- 単一選択
- 複数選択
- テキスト入力
- 数値日誌入力

表示する質問は `visibleFieldIds()` で制御します。

例:

- 最終排便が4日以上前、または水様便なら、腹痛・嘔吐・食欲機嫌を追加確認
- 硬便、痛み、がまんがあれば、出血や便付着を追加確認
- 薬が飲みにくい場合、飲みにくい理由を追加確認
- 薬が少なめ、忘れ、中止の場合、その後の変化を追加確認

回答によって非表示になった項目は、`pruneHiddenAnswers()` で送信データから除外します。

---

## 7. 現在の便秘問診の主な質問項目

### 7.1 2-3歳向け基本質問

- 最後にうんちが出たのはいつか
- 最近の排便間隔
- 便の硬さ
- 排便時の痛み
- がまんしている様子
- 便秘薬の飲み方

追加条件により以下を確認します。

- 腹痛・お腹の張り
- 嘔吐
- 食欲・機嫌
- 血がつくか
- パンツやおむつに便がつくか
- 薬が飲みにくい理由
- 薬を減らした、忘れた、中止した後の変化

### 7.2 0-1歳向け

- 最終排便
- 便性状
- 排便時の様子
- 便秘が気になる時期
- 出生時・1か月健診での指摘
- 哺乳・食事
- ミルク・母乳・乳製品との関係
- お腹や全身状態
- 体重・成長
- 便秘に対して使っているもの

### 7.3 4歳以降向け

- 最終排便
- 便性状
- 排便時痛・困りごと
- がまん・排便回避
- 下着汚れ・便失禁
- 園・学校で排便できるか
- 排尿・夜尿・昼間尿失禁
- 腹部症状
- 食事・水分・生活リズム
- 便秘薬
- 発達相談、通院中の病気、他院の薬
- 本人・保護者の困りごとや薬への不安

---

## 8. 患者向け出力

問診完了後、患者側には以下を表示します。

### 患者用メモ

患者・保護者があとで相談内容を思い出すためのメモです。

重要な制限:

- 薬の量は決めない
- 薬を増やす、減らす、やめる、再開する判断は医師と相談する
- 治療方針を患者へ直接指示しない

### 回答コード

`generateShortQrPayload()` で短いテキスト形式を生成します。

形式例:

```text
v1|pid=00123|tok=ABCD|q1=0|q2=2|q3=1|d=7,5,2,1,0,7
```

長い自由記載や医療判断は含めず、院内で必要になった場合のバックアップ用です。

---

## 9. 医師向け出力

`questionnaire.js` で以下を生成します。

- `generatePhysicianReview()`: 画面表示用の診察支援ダッシュボード
- `generateSummary()`: 医師向けミニサマリー
- `generateFacilityShare()`: 院内共有用テキスト
- `generatePatientMemo()`: 患者用メモ
- `generateSheetsVisitPayload()`: Googleスプレッドシート保存用JSON
- `generateShortQrPayload()`: 短縮回答コード

医師向けダッシュボードには以下を表示します。

- 確認区分・優先度
- 概要見出し
- 排便スナップショット
- 安全・追加確認
- 便秘薬の状況
- 直近日誌
- 診察で見るポイント
- AIが判断していないこと

「AIが判断していないこと」として、以下を明示しています。

- 診断
- 便塞栓の有無
- 処方量変更
- 治療中止可否
- 専門紹介の要否

---

## 10. 保存用JSON

患者画面からGoogle Apps Scriptへ送る保存用JSONは、概ね以下の構造です。

```json
{
  "patient_id": "00123",
  "visit_id": "20260531-00123-ABCD",
  "visit_token": "ABCD",
  "submitted_at": "2026-05-31T00:00:00.000Z",
  "age_years": "",
  "age_months": "",
  "age_profile": "toddler",
  "questionnaire_version": "toddler-mvp-v1",
  "questionnaire": {
    "q1_last_bowel_movement": "昨日",
    "q2_bowel_frequency": "2-3日に1回くらい"
  },
  "diary": {
    "diary_days_recorded": 7,
    "diary_bowel_days": 5
  },
  "outputs": {
    "urgency_level": "watch",
    "urgency_label": "診察で確認",
    "headline": "医師向け概要",
    "check_items": ["診察で見るポイント"],
    "not_judged": ["診断", "処方量変更"],
    "summary_text": "医師向けサマリー",
    "facility_share_text": "院内共有用テキスト",
    "patient_memo_text": "患者用メモ"
  }
}
```

実際には、表示されている質問だけが `questionnaire` に入り、非表示の条件分岐回答は送信しません。

---

## 11. Google Apps Scriptの役割

`apps-script/Code.gs` が、保存、履歴取得、医師画面、患者台帳、固定QR認証を担当します。

### 11.1 GETで扱う主なaction

- `patientHistory`: 患者履歴をJSONで取得
- `chatGPTContext`: ChatGPT相談用テキストを生成
- `emrText`: 電子カルテ貼り付け用テキストを生成
- `doctorHistory`: 医師向け履歴画面
- `doctorEntry`: 医師用の入力・確認画面
- `patientProfile`: 患者台帳編集画面
- `patientProfileData`: 患者台帳情報をJSONで取得
- `fixedQrAuth`: 固定QRモードの患者認証

### 11.2 POSTで扱う主な処理

- 患者問診の保存
- 処方履歴の保存
- トイレトレーニング履歴の保存
- 週次日誌の保存
- 医師メモの保存
- 患者台帳の保存

通常の患者問診保存では、JSONを受け取り `submitVisit(payload)` で保存します。

---

## 12. Googleスプレッドシートのシート構成

現在のシート構成は以下です。

### patients

患者台帳です。

- `patient_id`
- `age_years`
- `age_months`
- `created_at`
- `note`
- `birth_date`
- `background_history`
- `background_flags`
- `background_status`
- `background_updated_at`

### visits

受診ごとの問診記録です。

- `visit_id`
- `patient_id`
- `visit_token`
- `submitted_at`
- `saved_at`
- `urgency_level`
- `urgency_label`
- `headline`
- `questionnaire_json`
- `diary_json`
- `summary_text`
- `facility_share_text`
- `patient_memo_text`
- `reviewed_by_doctor`
- `doctor_note`
- `age_profile`
- `age_text_at_visit`
- `questionnaire_version`

### prescriptions

処方履歴です。

- `prescription_id`
- `patient_id`
- `date`
- `medicine_name`
- `dose`
- `instruction`
- `doctor_note`

### toilet_training

便秘症固有のトイレトレーニング履歴です。

- `patient_id`
- `date`
- `training_status`
- `diaper_status`
- `toilet_refusal`
- `note`

### diary_weekly

週次・直近日誌です。

- `patient_id`
- `period_start`
- `period_end`
- `recorded_days`
- `bowel_days`
- `longest_no_bowel_days`
- `hard_days`
- `pain_days`
- `withholding_days`
- `soiling_days`
- `med_taken_days`
- `note`

### DailyPIN

固定QR用の当日確認コードです。

- `date`
- `pin`
- `enabled`
- `form`
- `note`

---

## 13. 患者ID・来院トークン・visit_id

### 患者ID

- 5桁数字
- URLでは `patient_id` または `pid`
- HTML側でもApps Script側でも数字だけに正規化
- スプレッドシートでは先頭ゼロ落ちを避けるためテキスト扱い

### 来院トークン

- 英数字
- 最大12文字
- URLでは `visit_token` または `token`
- 個別QRの場合はQR作成画面で生成
- 固定QRの場合は認証後にApps Script側で生成

### visit_id

受診ごとの一意IDです。

明示されていなければ、概ね以下の形式で生成します。

```text
YYYYMMDD-patient_id-visit_token
```

Apps Script側でも不足時に生成・正規化します。

---

## 14. 保存処理とエラー処理

患者画面では、まず `fetch()` でGoogle Apps ScriptへPOSTします。

```text
HTML/JavaScript
  -> fetch POST
  -> Apps Script doPost
  -> submitVisit
  -> Google Sheets
```

成功時:

- 「院内保存が完了しました。画面を閉じても大丈夫です」と表示
- 来院IDを表示

失敗時:

- CORSや環境差を考慮し、hidden form + iframe送信へフォールバック
- 結果を直接確認できない場合は「院内保存リクエストを送信しました。受付または医師に確認してください」と表示
- 両方失敗した場合は、患者用メモ、回答コード、JSONを使って確認できるようにする

---

## 15. ChatGPT相談用テキスト生成

Apps Scriptの `chatGPTContext` actionで、患者の過去履歴をChatGPTへ貼るためのテキストを生成します。

モードは2つあります。

### preVisit

診察前整理モードです。

目的:

- 前回からの変化点
- 改善点・悪化点
- 医師が追加確認すべきこと
- 矛盾または不足している情報

### treatmentReview

医師向け治療方針検討モードです。

目的:

- 現在の状態評価
- 方針候補
- 各方針の理由
- 避けたい方針
- 次回再診間隔の考え方
- 保護者への説明文案

注意書きとして、ChatGPTには以下を明示しています。

- 最終的な診断や処方は医師が行う
- 患者・保護者へ直接指示する文体にしない
- 処方量変更の具体的数値指示は出さない
- 不確実な点は推測で埋めない

---

## 16. 他テーマへ流用するときに残すべき共通部分

別テーマへ流用しても、以下は共通基盤として残すのがよいです。

- QRコードまたはURLから患者入力画面を開く
- 患者IDを5桁数字として扱う
- 来院トークンで受診単位を区別する
- 患者画面で必須回答チェックを行う
- 条件分岐で必要な追加質問だけ出す
- 非表示になった回答は送信しない
- 患者向け出力では自己判断を促さない
- 医師向け出力では確認点と不足情報を整理する
- Google Apps Scriptで保存前検証を行う
- Googleスプレッドシートに `patients` と `visits` を共通基盤として残す
- 保存中、保存完了、保存失敗を患者に明確に表示する
- ChatGPT用テキストでは、最終判断は医師が行うと明示する

---

## 17. 他テーマへ流用するときに差し替える部分

以下はテーマごとに差し替える必要があります。

### 患者向け画面

- タイトル
- イントロ文
- 説明文
- 質問文
- 選択肢
- 患者用メモ

### 質問ロジック

- 基本質問
- 条件分岐
- 必須項目
- 自由記載欄
- 数値入力欄
- 回答コードの項目

### 医師向けロジック

- 優先確認度
- 見出し
- スナップショット項目
- 危険サイン・注意サイン
- 診察で見るポイント
- AIが判断していないこと
- 院内共有文
- 電子カルテ貼り付け文
- ChatGPT相談用コンテキスト

### Googleスプレッドシート

- 疾患・テーマ固有の履歴シート
- 日誌カラム
- 医師入力フォーム
- 薬剤や処置のプリセット
- 背景情報フラグ

---

## 18. 屋根養生QRアンケートへ応用する場合の置き換えイメージ

便秘問診の構造を、屋根養生の事前アンケートに置き換える場合の例です。

### 共通構造として使える部分

- QRコードからアンケート画面を開く
- 受付番号・案件番号・顧客IDなどをURLに入れる
- 固定QR + 当日確認コードのような簡易認証を使う
- 回答内容に応じて追加質問を出す
- 回答後に顧客向け控えメモを出す
- 社内向けサマリーを作る
- スプレッドシートへ案件履歴として保存する
- ChatGPTへ貼るための現場確認コンテキストを作る

### 置き換える項目例

便秘システムの `patient_id` は、屋根養生では以下に置き換え可能です。

- 顧客ID
- 案件番号
- 受付番号
- 見積番号

便秘システムの `visit_token` は、以下に置き換え可能です。

- 当日受付トークン
- 現場確認トークン
- 案件別確認コード

便秘システムの年齢プロファイルは、以下に置き換え可能です。

- 戸建て / 集合住宅 / 店舗
- 平屋 / 2階建て / 3階建て以上
- 応急養生 / 雨漏り調査 / 修理前確認
- 台風後 / 経年劣化 / 工事前後

### 屋根養生向けの質問セット例

基本質問:

- 建物種別
- 屋根の形状や階数
- 雨漏りの有無
- 雨漏りしている場所
- いつから症状があるか
- 強風・台風・落下物などのきっかけ
- 室内被害の有無
- 電気設備や天井周りの危険の有無
- すでに業者が入っているか
- 写真を撮影できるか

条件分岐例:

- 雨漏りあり → 室内の漏水場所、量、電気周りの危険を追加確認
- 台風・強風後 → 飛散物、近隣被害、保険相談の有無を追加確認
- 2階建て以上 → はしご・足場・安全確認を追加
- 室内天井にシミあり → 天井材のたわみ、照明器具付近かを追加確認
- 応急養生希望 → 希望日時、立ち会い可否、駐車スペースを追加確認

社内向けサマリー例:

- 緊急度
- 雨漏りの有無
- 室内被害
- 屋根上作業の安全リスク
- 必要そうな資材
- 現地確認で見るポイント
- 顧客へまだ確認すべきこと

患者用メモに相当する顧客向けメモでは、以下のように書き換えます。

- 回答内容の控え
- 現地確認時に伝えること
- 危険な場所には近づかない
- 屋根に上がらない
- 応急養生や修理可否は現地確認後に判断する

---

## 19. ChatGPTへ相談するときの依頼文例

以下のように依頼すると相談しやすいです。

```text
以下は、現在小児便秘問診として作っているQRコード事前問診システムの設計です。
この仕組みを、屋根養生の事前アンケートに横展開したいです。

やりたいこと:
- 顧客がQRコードから事前アンケートを入力する
- 回答内容に応じて追加質問を出す
- 社内スタッフ向けに緊急度、現地確認ポイント、必要資材の候補を整理する
- Googleスプレッドシートへ案件履歴として保存する
- 顧客向けには自己判断や危険行動を促さない控えメモを出す

相談したいこと:
1. 便秘問診のどの部分を共通基盤として残すべきか
2. 屋根養生向けにどの質問項目を設計すべきか
3. 条件分岐をどう組むべきか
4. スプレッドシートのシート構成をどう変えるべきか
5. 顧客向けメモと社内向けサマリーをどう分けるべきか
6. 安全面・個人情報面で注意すべきこと

以下に現在の仕組みを貼ります。
```

---

## 20. 実装時の注意

屋根養生へ流用する場合、医療用の文言や便秘固有のシートはそのまま使わず、必ずテーマに合わせて置き換えます。

特に注意する点:

- 顧客に屋根へ上がる行動を促さない
- 危険箇所へ近づく指示を出さない
- 応急養生や修理可否をアンケートだけで断定しない
- 現地確認が必要な判断は社内向けサマリーに留める
- 写真アップロードを追加する場合は、保存先と個人情報管理を別途設計する
- Googleスプレッドシートに住所、電話番号、写真URLなどを入れる場合はアクセス権限を慎重に設定する

---

## 21. まとめ

現在の便秘問診システムは、以下の汎用パターンとして再利用できます。

```text
QRコードで事前入力
  -> 条件分岐アンケート
  -> 顧客・患者向け控え
  -> 担当者・医師向けサマリー
  -> Googleスプレッドシート保存
  -> 履歴確認
  -> ChatGPT相談用コンテキスト生成
```

屋根養生へ応用する場合は、医学的判断の部分を「現場安全確認・緊急度整理・必要資材候補・追加確認事項」に置き換えるのが基本方針です。
